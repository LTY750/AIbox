# AIbox Mobile（Android）UI 全面审查报告

- **审查日期**：2026-09-04
- **审查方式**：Pixel 7 模拟器真机运行（API 35，浅色/深色/横竖屏）+ 源码静态审查（`src/renderer`、`android/`）
- **审查范围**：页面设计 / 交互设计 / 动画设计；仅分析评估，未修改任何代码
- **应用版本**：0.0.1（包名 `com.aibox.mobile`，Capacitor WebView + React 渲染层）

---

## 一、总体结论

应用整体走的是**极简聊天工具**路线：核心聊天页、Markdown/KaTeX 渲染、深色模式、双主题预设等完成度较高，工程上也有安全区适配、reduced-motion 支持、错误分类提示等成熟做法。但存在 **1 个严重问题（横屏初始渲染失败）** 和若干一致性问题，主要根因是 **MUI + Mantine + Tailwind 三套 UI 体系混用**，以及部分移动端状态（空态、加载态、键盘态）处理不完整。

| 维度 | 严重 | 中等 | 轻微 |
|---|---|---|---|
| 页面设计 | 1 | 4 | 5 |
| 交互设计 | 0 | 5 | 3 |
| 动画设计 | 0 | 2 | 2 |

---

## 二、页面设计

### 问题清单

**P1【严重】横屏初始渲染失败，内容被挤压至左半屏**
- 位置：会话页（竖屏旋转至横屏时）
- 现象：旋转后整个 UI 只渲染在屏幕左侧约 45% 宽度内，右半屏为空白背景；需等待数秒或手动滑动后才切换为正常的「侧边栏 + 内容」双栏布局（已截图留存 `16-landscape.png` vs `16c-landscape-scroll.png`）。
- 分析：`useScreenChange.ts` 基于 MUI `useMediaQuery` 断点切换 `windowSizeClass`，旋转瞬间 WebView 视口尺寸与媒体查询/侧边栏状态（`showSidebar`）更新存在时序竞态，首帧布局错误。
- 建议：监听 Capacitor 的 `resize`/orientation 事件，在旋转完成前隐藏内容或延迟一帧渲染；或为 `windowSizeClass` 切换增加防抖与布局过渡，保证旋转过程不出现错误中间态。

**P2【中等】三套 UI 框架混用，控件风格不统一**
- 位置：全局（`__root.tsx` 同时挂载 MUI `ThemeProvider` 与 Mantine `MantineProvider`，外加 Tailwind）
- 现象：
  - 设置页下拉框为 Mantine `NativeSelect` 样式（胶囊 + 上下箭头，视觉接近原生 Web 控件）；
  - 主题/解析器选择走自定义 `AdaptiveSelect` 底部弹层（移动端体验好）；
  - 「+」附件菜单、会话菜单又是另一套自定义浮层（一个有图标、一个纯文字）。
  - MCP 页「添加服务器」为虚线按钮，模型提供方页「添加」为描边按钮，风格不一致。
- 建议：收敛移动端组件规范——下拉选择统一走 `AdaptiveSelect` 底部弹层；操作菜单统一带图标；按钮变体（虚线/描边/填充）建立明确语义并全局一致。

**P3【中等】设置子页面标题均为「设置」，层级定位弱**
- 位置：设置 → 模型提供方 / 文档解析器 / MCP / 常规设置等所有二级页
- 现象：顶栏标题恒为「设置」，有的页面靠正文第一行大标题补充（常规设置、MCP 设置），有的页面完全没有（模型提供方、OpenAI 详情页三级页也只有「设置」）。
- 建议：二级/三级页顶栏直接显示当前页面名（如「模型提供方」「OpenAI」），与返回箭头配合形成清晰的路径感。

**P4【中等】底部免责声明文字被系统手势条遮挡**
- 位置：主页 / 会话页底部「AI 生成的内容可能不准确，请核实重要信息。」
- 现象：在手势导航模式下，该行文字与手势条重叠，文字下半部分被裁切（浅色/深色模式均复现）。工程上已有 `mobile_safe_area.ts` 与 CSS 安全区变量体系，但该行文字所在容器未应用底部安全区。
- 建议：为输入区下方的免责声明容器加上 `padding-bottom: max(env(safe-area-inset-bottom), var(--mobile-safe-area-inset-bottom))`，或将免责声明并入输入框容器内部。

**P5【中等】多语言混杂：中文界面出现整段英文说明**
- 位置：设置 → 文档解析器（LlamaParse 描述「Tries local parsing first...」整段为英文）
- 分析：`DocumentParserSettings.tsx` 的 `PARSER_DESCRIPTIONS` 文案未进入 zh-Hans 翻译表（`translation.json` 无对应条目）。
- 建议：补齐各解析器描述的中文翻译，并建立「新增文案必须进 i18n 扫描」的约束（仓库已有 `for-key-scan.ts` 机制，可直接利用）。

**P6【轻微】消息气泡下方显示「unknown」模型名**
- 位置：会话页历史消息元信息行
- 分析：内置示例会话数据 `initial_data.ts` 中 `model: 'unknown'`，且 `Message.tsx` 在 `msg.model` 缺失时回退展示字面量 `'unknown'`。
- 建议：模型名缺失时不渲染该标签，或回退为会话当前模型名。

**P7【轻微】「我的搭档」卡片头像加载失败，呈灰色空圈**
- 位置：我的搭档 → AIbox Mobile 精选列表（简历格式优化助手、正则表达式生成器等多个卡片头像为灰圈，仅个别有图标）
- 建议：检查头像资源加载链路；加载失败时使用首字母/默认图标兜底，避免一排灰圈造成的「未完工」观感。

**P8【轻微】侧边栏头部展示版本号「0.0.1」**
- 位置：侧边栏顶部「AIbox Mobile 0.0.1」
- 建议：版本号移至「关于」页；头部保持品牌区简洁。

**P9【轻微】设置首页下半屏大面积留白**
- 位置：设置首页（9 个列表项约占半屏，其余全空白）
- 建议：列表项补齐（如主题、字体等常用项上提）或为列表增加分组卡片样式，减少「悬空」感。

**P10【轻微】空状态文案措辞生硬**
- 位置：模型选择抽屉「没有可用的合格模型」；我的搭档「没有搭档。创建您的第一个搭档!」（半角感叹号）
- 建议：统一空状态文案规范（完整中文标点、口语化），例如「暂无可用模型」。

### 设计亮点（页面设计）

1. **深浅双主题完成度高**：切换即时生效，聊天气泡、表格、KaTeX、代码块在深色下对比度均正常。
2. **预设主题色 + 字体大小滑杆**（常规设置）：个性化能力超出同类应用平均水平。
3. **聊天内容渲染质量高**：Markdown 表格、KaTeX 公式、代码高亮渲染精致，是核心竞争力的体现。
4. **模型提供方列表**：品牌图标统一为圆形彩色图标，识别度高；模型行的能力图标（思考/视觉/工具/上下文长度）信息密度设计得好。
5. **图片生成器空状态**：图标 + 标题 + 引导文案 + 5 个建议标签，是全应用最完整的空状态设计，可作为其他页面空状态的范式。

---

## 三、交互设计

### 问题清单

**I1【中等】键盘弹起瞬间输入框被完全遮挡，随后才跳动归位**
- 位置：会话页 / 主页输入区
- 现象：点击输入框后约 1–1.5 秒内，键盘完全盖住输入栏（用户在盲打），随后布局才 resize 到位；`AndroidManifest` 已配置 `adjustResize`，但 WebView 的 resize 存在可见延迟与跳变。
- 建议：配合 Capacitor Keyboard 插件监听 `keyboardWillShow`，提前为输入区应用底部 padding 过渡动画，消除「先遮挡后跳变」的体验断层。

**I2【中等】打开会话出现整屏白屏，无任何加载态**
- 位置：侧边栏点击会话 → 会话页
- 现象：点击「Markdown 101 (Example)」后白屏约 2–3 秒才渲染出内容，期间无骨架屏、无 Spinner。
- 分析：`router.tsx` 未配置 `pendingComponent`，路由/数据加载期间页面为空白。
- 建议：为会话页加载增加骨架屏（消息气泡形状占位）或至少居中 Spinner；本地数据读取应尽量同步命中缓存后再渲染。

**I3【中等】空状态是「死胡同」，缺少行动引导**
- 位置：模型选择抽屉（无可用模型时仅一行文字）、图片生成器输入栏（「没有可用模型」）、MCP 服务器列表
- 现象：用户未配置 API Key 时，多处入口只告知「没有」，不提供「去设置」的跳转。
- 建议：所有「无资源」空状态增加主行动按钮（如「添加模型提供方」），点击直达对应设置页。

**I4【中等】底部弹层选项无选中态标识**
- 位置：文档解析器类型、主题选择的底部弹层
- 现象：当前生效项在弹层中无对勾/高亮，用户无法确认当前选择。
- 建议：`AdaptiveSelect` 弹层为当前项增加勾选图标或高亮底色。

**I5【中等】设置首页「+ 添加」按钮与手势条区域过近**
- 位置：模型提供方页底部「+ 添加」按钮，与手势条几乎重叠，易误触且视觉拥挤。
- 建议：底部固定按钮统一应用安全区 padding（同 P4）。

**I6【轻微】会话菜单与「+」菜单规范不一**
- 位置：会话页「···」菜单（纯文字列表）vs 输入区「+」菜单（图标 + 文字）
- 建议：统一为图标 + 文字；危险操作（清空/删除）保持红色但建议增加二次确认的一致性检查。

**I7【轻微】搭档页「我创建的搭档」空状态仅一行灰字**
- 建议：参考图片生成器空状态，增加图标与引导按钮（旁边虽有「创建」按钮，但视觉权重太弱）。

**I8【轻微】输入框占位文案与场景重复**
- 位置：主页空状态已有「今天我能为你提供什么帮助？」，输入框占位又是「在这里输入你的问题...」，语义重复。
- 建议：占位文案可更轻量（如「发消息…」），避免同一屏两句引导语。

### 设计亮点（交互设计）

1. **导航返回体系完整**：`mobile_back_navigation.ts` 统一接管 Android 返回键，实测弹层 → 页面 → 主页的返回层级正确，无「返回即退出」问题。
2. **侧边栏支持手势滑动打开/关闭**（MUI SwipeableDrawer），且当前会话在列表中高亮，定位清晰。
3. **「+」按钮点击后旋转为「×」**，附件菜单带图标的弹出层，是不错的微交互。
4. **错误处理体系成熟**：`MessageErrTips.tsx` 按错误类型（上下文超限、OCR、HTTP 状态码、requestId）分类展示，`MessageLoading` 有 LoadingBubble / RetryingIndicator，重试路径完整。
5. **消息操作条**（重新生成/编辑/复制/更多）收纳为胶囊工具条，不打断阅读流。
6. **发送按钮禁用态**（空输入时置灰）表达明确；token 用量（↑ 261）实时可见。

---

## 四、动画设计

### 问题清单

**A1【中等】页面间无任何转场动画**
- 位置：全局路由切换（主页 ↔ 会话 ↔ 设置各级页面）
- 现象：所有页面切换均为瞬时硬切，无滑动/淡入过渡；设置多级钻取时没有方向感，加剧 I2 白屏问题的感知。
- 建议：为移动端路由增加轻量转场（二级页右滑进入、返回左滑退出，时长 200–250ms，遵循 reduced-motion 时降级为瞬时）。

**A2【中等】键盘 resize 无过渡，布局跳变生硬**
- 同 I1，动画维度补充：输入区从「被遮挡」到「归位」是瞬间跳变，没有任何插值动画。
- 建议：为输入区位移加 150–200ms 的 transform 过渡，与系统键盘动画时长对齐。

**A3【轻微】`transition-all` 的滥用存在性能与预期外动画风险**
- 位置：`Artifact.tsx`（`transition-all duration-300`）、`InputBox/Attachments.tsx`、`HistoryPanel.tsx` 等
- 建议：明确指定过渡属性（颜色/阴影/变换），避免布局属性被意外动画化导致掉帧。

**A4【轻微】白屏与内容出现之间无淡入衔接**
- 位置：会话页加载完成后内容直接闪现。
- 建议：内容就绪后以 150–200ms 淡入，掩盖加载抖动（仓库已有 `animate-in fade-in` 工具类可直接复用）。

### 设计亮点（动画设计）

1. **动画时长体系统一**：全局过渡集中在 100–300ms（抽屉 200ms、悬停 150–200ms、淡入 300ms），符合 Material 建议区间，无过度动画。
2. **系统级 reduced-motion 支持到位**：`usePrefersReducedMotion` 在根布局、侧边栏、抽屉、流式文本、平滑滚动等多处生效，无障碍考虑周全。
3. **流式输出动画细腻**：`streaming-text-fade` 新文本淡入 + `smooth-follow-output` 平滑跟随滚动，是 AI 聊天应用的关键体验，实现质量高。
4. **按钮按压反馈**：设置项 `active:scale-[0.97]`（带 `motion-reduce` 降级），触感反馈符合移动端预期。
5. **侧边栏开合动画**与内容区 padding 过渡联动（MUI `transitions` enteringScreen/leavingScreen 时长差异化），开合方向感正确。

---

## 五、优先级建议汇总

| 优先级 | 事项 |
|---|---|
| 立即修复 | P1 横屏渲染失败；I2/P4 加载白屏与安全区遮挡 |
| 下个迭代 | I1/A2 键盘体验；I3 空状态引导；P3 页面标题；P5 i18n 补全；A1 页面转场 |
| 持续改进 | P2 组件体系统一；P6–P10、I6–I8、A3–A4 细节打磨 |

> 本报告仅为分析与建议，未对代码做任何修改。真机截图存于 `.workbuddy/review-shots/`。
