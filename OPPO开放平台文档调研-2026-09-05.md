# OPPO 开放平台文档调研 — 对 AIbox Mobile 的借鉴价值

调研日期：2026-09-05
调研对象：OPPO 开放平台（open.oppomobile.com）官方文档、开发者指南、API 参考、设计规范
调研对象项目：AIbox Mobile（Capacitor + React，applicationId `com.aibox.mobile`，minSdk 23 / targetSdk 36 / compileSdk 36）

---

## 0. 检索方式说明（务必先读）

OPPO 文档站是纯前端渲染 SPA（`/documentation/page/info?id=xxxx` 与 `/new/developmentDoc/info?id=xxxx` 两种路由，页面 HTML 只有 7KB 的壳，正文由 federation 远程 JS 加载）。直接抓取拿不到正文，Chromium 自动化因下载源超时未能完成安装。

因此本报告的正文来自**搜索引擎对 OPPO 官方文档页面的索引快照**（索引正文与官方原文一致，含配置项、字段表、代码片段），并对关键结论做了**双源交叉验证**（官方 BBS 文档贴、个推/ZEGO/腾讯云 IM 等接入方的官方适配文档、金标联盟成员厂商同源规范）。

**可信度分级**：
- 高 — 多源一致的接口字段、配置规则、量级限制
- 中 — 单一来源或版本可能漂移（如 SDK 版本号、aar 文件名）
- **落地前请以开放平台页面实际显示为准**，特别是 SDK 版本与后台字段。

---

## 1. 结论先行：三个必须现在处理的问题

| # | 问题 | 依据文档 | 风险 |
|---|---|---|---|
| 1 | **`targetSdk 36` + 未配置 `windowLayoutInDisplayCutoutMode` = 崩溃风险** | 《Android 15 应用兼容性适配指导》id=13047 | 文档原文：targetSdk≥35 强制 Edge-to-edge；非浮窗应用未配置 `LAYOUT_IN_DISPLAY_CUTOUT_MODE_ALWAYS` 会抛 `IllegalArgumentException`。当前 `styles.xml` 三个主题**均未声明该属性**。项目又已在 `MainActivity` 手动 `setDecorFitsSystemWindows(false)`，两处叠加 |
| 2 | **AI 生成内容标识 + 投诉举报入口缺失，商店会直接驳回** | 《OPPO 应用审核规范》id=12131 第二十五/二十七条 | AI 问答类 = 生成式人工智能服务；硬性要求：交互界面显著标识"本内容由AI生成"、提供投诉举报入口、青少年模式、风险内容屏蔽 |
| 3 | **`POST_NOTIFICATIONS` 已声明但无触达链路** | 《权限申请说明填写指引》id=13449、《设备与应用通知权限使用及申请标准》 | 金标联盟（OPPO/vivo/小米/荣耀）统一规范已执行：申请需说明最高频目的+功能+1080×1920 入口截图。且国内 OPPO 设备无 GMS，现有 `google-services.json` 路径走不通，需 OPush 通道 |

---

## 2. 文档清单（按优先级）

### P0 — 决定能否上架 / 有崩溃风险

#### 2.1 《Android 15 应用兼容性适配指导》
`https://open.oppomobile.com/documentation/page/info?id=13047`（另有 OTalk 解读贴 tid=6274）

**核心要点**
- **Edge-to-edge**：targetSdk ≥ 35 且未申请豁免 `windowOptOutEdgeToEdgeEnforceMent` 时**默认强制开启**。状态栏默认透明、三键导航不透明度 80%；`setStatusBarColor` / `setNavigationBarColor` 已废弃对 Android 15 无效。
- **挖孔（cutout）**：非浮窗场景 `layoutInDisplayCutoutMode` **必须**为 `LAYOUT_IN_DISPLAY_CUTOUT_MODE_ALWAYS`，否则应用崩溃并抛 `IllegalArgumentException`。
- **Manifest 限制**：字段 tag 数量受限（如 `<application>` 下 `<activity>` ≤ 3000）；attr string 长度与特殊字符受限；包名仅允许 `a-zA-Z0-9._`。
- **前台服务**：targetSdk 35+ 的 `BOOT_COMPLETED` 接收方不允许启动 `dataSync` / `camera` / `mediaPlayback` / `phoneCall` / `mediaProjection` / `microphone` 类型前台服务，否则 `ForegroundServiceStartNotAllowedException`。
- **最低可安装 targetSdk**：Android 15 机器上 <24 无法安装。
- ART 库符号可见性、16KB page size、预测性返回、JobScheduler 最早/最晚触发间隔 <15 分钟抛异常、Configuration 不再排除系统栏。

**适用场景**：所有 targetSdk ≥ 35 的 Android 应用，尤其自定义了系统栏/全屏的应用。

**可借鉴之处（对本项目）**
- 项目 `targetSdk 36`，`MainActivity.configureEdgeToEdge()` 已在手动做全屏，`styles.xml` 把 statusBar/navigationBar 设为 transparent —— **与系统强制 E2E 叠加后行为需重新验证**。
- **必须补 `android:windowLayoutInDisplayCutoutMode="shortEdges"` 或 `always`**（AppCompat 主题下用 `shortEdges` 更稳，但 OPPO 文档要求非浮窗必须 ALWAYS；建议按文档配 always 并在折叠屏/横屏/挖孔机实测）。
- 项目有 `StreamHttpForegroundService`（`foregroundServiceType=dataSync`、`stopWithTask=false`）—— 文档明确 dataSync 类型在 BOOT_COMPLETED 后启动会抛异常。若未来从开机广播拉起流式任务，需改为用户触发路径。
- `enableOnBackInvokedCallback="true"` 已开（预测性返回已适配 ✓），这条合规。
- 16KB page size：需在发布前用 16KB 对齐的 ELF 验证，Capacitor/so 库是重点排查对象。

---

#### 2.2 《OPPO 应用审核规范》
`https://open.oppomobile.com/documentation/page/info?id=12131`

**核心要点**
- 总则：互联网信息服务管理"九不准"。
- 基础信息：应用名称 ≤15 汉字/30 英文字符、名称须与 APP 备案名一致、建议与软著名一致；ICON 512×512 直角不切圆角、无描边、无角标（禁"官方/正版/OPPO/美女"等）；截图 1080×1920 ≥3 张；包名发布后不可改；**不支持仅 32 位包体**，支持 64/32 双包在架。
- 功能规范：不可常驻通知栏或无法清除；**不可启动后强制更新**；敏感权限需先征得同意，**用户拒绝后不可强制退出**；定向推送需提供关闭入口。
- 安全：恶意行为（自动下载/远程控制/权限滥用/尝试 root/**动态加载恶意插件**/剪切板劫持/拒绝服务）、黑灰产、欺骗行为（伪装系统/界面/通知/钓鱼/隐蔽扣费）。
- **生成式 AI 专属条款（第二十五条，对本项目最关键）**：
  - AI 问答类、AI 创作类、AI 绘画类（未采集人体生物信息）**属于生成式人工智能服务**；
  - 需采取添加**青少年模式**、在**用户输入端与内容生成端**对风险内容进行屏蔽等手段；
  - 需提供**投诉、举报机制及便捷入口**；
  - 在生成文本/音频的交互场景界面需有**显著提示标识**（如"本内容由AI生成"），或在生成内容的起始/末尾/中间适当位置有文字、语音、通用符号或音频节奏提示。
- 深度合成服务（第二十七条）：开始前**单独**向用户申请授权才能采集人脸/人声等生物信息；生成内容需在合理位置显著标识；同样要求投诉举报入口 + AI 生成标识。
- 常见驳回：应用内**无账号注销功能**（注销≠退出，须在功能界面有真实有效通道）。

**适用场景**：所有拟上架 OPPO 软件商店的应用，AI 类应用为高审查类目。

**可借鉴之处（对本项目）**
- AIbox 属于典型的"AI 问答类"，第二十五条是**硬性驳回项**。需要在渲染层做三件事：①消息流中 AI 回复的持久性标识；②设置里的举报入口；③青少年模式。
- "用户输入端与内容生成端双向风险屏蔽" —— 提示词侧与输出侧都要有过滤。
- 若未来做语音输入/语音播报（生物信息相关），第二十七条的单次授权 + 标识要求会适用。
- "不可常驻通知栏" 与项目的 `StreamHttpForegroundService` 相关：前台服务通知需可清除、文案需真实描述用途。
- 包体：需提供 64 位包（或双包）。

---

#### 2.3 《权限申请说明填写指引》 + 各权限使用及申请标准
`https://open.oppomobile.com/documentation/page/info?id=13449`
同类：通讯录权限 id=13701、短信、通话记录、剪贴板、所有文件访问、读取文档、读取音频、读取图片视频、日历、设备管理器、**设备与应用通知权限**、位置、附近的设备

**核心要点**
- 金标联盟（ITGSA：OPPO/vivo/小米/荣耀）**统一标准**，一家整改四家互认（但需在各自平台上传才生效）。
- 流程：开放平台 → 管理中心 → 移动应用列表 / 或 应用分发 → 应用管理 → **权限合规审核**；上传 APK 后**自动解析**全部敏感权限与普通权限；逐条选"最高频使用目的 + 最高频功能"；摄像头/麦克风/确切位置/大致位置还需上传 **1080×1920、JPG/PNG、<1M** 的功能入口截图。
- 标准条款（以通讯录标准 id=13701 为例，各权限同构）：
  - 申请权限时须**同步告知目的**，不得含广告或误导描述；
  - 严格按格式逐个声明；**仅需读权限时不得同时声明写权限**；
  - 用户拒绝/撤回后**不得强制退出**，且不影响无关功能；
  - **48 小时内弹窗索权不超过 1 次**；不得每次启动都索要；
  - **动态申请**：用户与功能互动时再请求，不得提前、不得一揽子申请。
- 处置措施：不合理的申请前期默认拒绝（需用户主动授权），终态直接**禁用**。
- 提供 **Picker 适配指南** —— 用系统 Picker 替代敏感权限申请。

**适用场景**：申请任一运行时敏感权限的 Android 应用。

**可借鉴之处（对本项目）**
- 项目已声明 `POST_NOTIFICATIONS`，**"设备与应用通知权限使用及申请标准"是直接对应条款**。通知权限的申请时机、文案、拒绝后的降级路径都要按标准设计。
- 项目还声明了 `FOREGROUND_SERVICE` / `FOREGROUND_SERVICE_DATA_SYNC` / `WAKE_LOCK` / `INTERNET`，加上 FileProvider 与分享导出能力 —— 若后续引入图片/文件读取，会命中"读取图片视频/读取文档/所有文件访问"标准，建议**优先走系统 Picker** 规避权限。
- 后台填写权限说明时要准备 1080×1920 的功能入口截图，这是纯 UI 工作，可提前排期。

---

#### 2.4 《OPPO合作游戏审核规范》的隐私政策章节（id=12277）
`https://open.oppomobile.com/documentation/page/info?id=12277`

**核心要点**（虽名为游戏规范，但这是 OPPO 站内对隐私政策要求最系统的成文表述，应用侧审核口径同源）
- 首次登录须主动弹窗提醒并征得同意；未弹窗时隐私政策**默认不勾选**。
- 协议**必须含拒绝选项**；用户拒绝后最多挽留两次，之后不得再挽留。
- App 内需有隐私政策模块；用户可**4 步操作内**找到入口。
- 用户协议与隐私政策**必须分开展示、独立成文**，隐私政策不可作为用户协议的一部分。
- 须标识发布/生效/更新日期；同意前后的内容须一致，变更后需重新征得同意。
- 后台填写的隐私政策网址，其关键信息（应用名称、日期）须与当前应用一致。
- 协议主体须为**个人信息处理者**，且原则上与开发者主体一致。
- 不得为账号注销设置不合理障碍。

**适用场景**：所有需要账号体系与数据收集的应用。

**可借鉴之处（对本项目）**
- 直接可作为 AIbox 隐私政策与首次启动弹窗的验收清单（弹窗 + 拒绝选项 + 4 步可达 + 独立成文 + 日期 + 主体一致）。
- "隐私政策网址与 APP 内完全一致" 是常见驳回点，需保证 renderer 内嵌文本与线上 URL 同步。
- 项目已有 `SecureStoragePlugin`（Keystore 背书）与 `mobile_settings_secrets.ts`，密钥/敏感设置的存储合规基础已在。

---

### P1 — 直接影响功能可用性

#### 2.5 OPPO 推送服务（OPush）文档族

| 文档 | 链接 | 定位 |
|---|---|---|
| 快速接入指南 | `.../documentation/page/info?id=11221` | SDK 集成全流程 |
| PUSH SDK 合规使用声明 | `.../documentation/page/info?id=11229` | 隐私合规 |
| 鉴权 | `.../documentation/page/info?id=11234` | 服务端鉴权接口 |
| 消息分类说明 | `.../new/developmentDoc/info?id=11227` | 私信/公信细则 |
| 推送服务受限说明 | `.../documentation/page/info?id=13190` | 量级/QPS/时段 |
| 单点推送 API | BBS tid=1927 / `.../new/developmentDoc/info?id=11237` 附近 | 服务端推送 |
| 客户端 SDK 接口文档 | BBS tid=1918 | 客户端 API |
| 服务端 Java SDK 接入指南 | BBS tid=1919 | 服务端 SDK |

**核心要点**

*接入（id=11221）*
- 权限申请通过后，在 OPPO 推送平台 → 配置管理 → 应用配置 取得 **AppKey / AppSecret / MasterSecret**（MasterSecret 仅主账号可见）。
- SDK 以 aar 形式提供（`com.heytap.msp_V3.7.1.aar`），minSdk 19，包体 <500KB，响应 <500ms。
- Manifest 需声明 `com.coloros.mcs.permission.RECIEVE_MCS_MESSAGE` 与 `com.heytap.mcs.permission.RECIEVE_MCS_MESSAGE`，注册 `CompatibleDataMessageCallbackService` 与 `DataMessageCallbackService`（`android:process=":push"`），并通过 `<meta-data>` 配置 app_key / app_secret。
- **Android 8.0（API 26）以上必须适配通知通道（channel）**，未指定通道的通知无法显示。
- 支持 ColorOS 3.1+ 的 OPPO 机型、一加 5/5T+、realme 全系。

*客户端 API（tid=1918）*
- `HeytapPushManager.init(context, needLog)` / `isSupportPush()` / `register(appContext, appKey, appSecret, ICallBackResultService)` / `setAppKeySecret()` / `getRegister()`（首次注册失败后重试）。
- `PushManager.getInstance().requestNotificationPermission()` 申请通知栏权限。
- 回调 `onRegister(responseCode, registerID)`，code 0 为成功。
- **混淆必加**：`-keep public class * extends android.app.Service` 与 `-keep class com.heytap.msp.** { *; }`。

*服务端鉴权（id=11234）*
- `POST /server/v1/auth`，`application/x-www-form-urlencoded`。
- `sign = SHA256(appKey + timestamp + masterSecret)`；`timestamp` 为 **13 位毫秒 Unix 时间戳，GMT+8**，须在**最近 10 分钟内**。
- 返回 `auth_token`，**默认有效期 24 小时**，后续所有 API 在 HTTP body 携带。

*推送 API*
- 单推：`POST /server/v1/message/notification/unicast`
- 批量单推：`POST /server/v1/message/notification/unicast_batch`
- 广播：先 `POST /server/v1/message/notification/save_message_content` 得 `message_id`，再 `POST /server/v1/message/notification/broadcast`
- 关键字段：`target_type`（1=ALL / 2=registration_id / 5=别名 / 6=标签）、`target_value`（多个用分号分隔，最多 1000）、`notification`、`verify_registration_id`（到达客户端后校验 regid，不一致则丢弃）、`notify_id`（相同 notify_id 的新消息覆盖旧消息，适合"AI 回复完成"这类状态刷新）。
- 广播标签支持表达式：`{"or":[], "and":[], "not":[]}`，先算 or → and → not，多项取交集。
- 通知体字段：`style`、`title`、`content`、`offLine`、`offLineTtl`、`pushTimeType`、`channelId`、`showTtl`、`notifyId`。

*消息分类（id=11227）*
- 新分类：**通讯与服务**（原私信）/ **内容与营销**（原公信）；2024-11-20 起新接入应用仅能用新分类，ColorOS 13+ 不再支持自建通道。
- 通讯与服务需**邮件申请**，且**必须提交私信模板**才能生效；模板用 `$$` 包裹可变量，且不能全为可变量（须含固定文案）。
- 处罚梯度：一次警告 + 7 日整改；二次关闭私信权限 15 天；三次 30 天；三次以上**永久禁用**。通讯通道发送非聊天消息 → **永久禁用**。
- 订阅类消息要求最严：需独立且**默认关闭**的推送开关、用户主动订阅行为、界面明确提示"您将收到 xx 消息推送"、推送内容需带"您订阅的…"字样。

*受限说明（id=13190）*
- 正式权限：通讯与服务不限量；内容与营销 = 累计用户数×2/日（<1万按 2 万/日），单设备 2 条/日（新闻类 5 条）。
- 测试权限：内容与营销 1000/日，通讯与服务**不支持**。
- QPS：仅公信受限，<500万 2000/s；500万–1000万 3000/s；≥1亿 5000/s。
- 单用户接收上限 2000 条/日。
- **自 2024-07-29 起，非 OPPO 软件商店官方渠道下载的应用不再提供公信消息服务**。
- 测试设备最多配置 50 个，不受单设备条数限制。

*合规声明（id=11229）*
- 须在隐私政策中披露：第三方 SDK 名称（OPPO PUSH SDK）、公司（广东欢太科技有限公司）、收集信息种类（应用基本信息、应用包名、RegistraterID、appKey、appSecret）、使用目的、隐私政策链接。
- **延迟初始化要求：必须在取得最终用户同意后才能调用 `init` 接口**，避免未同意前处理个人信息。

**适用场景**：需要国内 OPPO/一加/realme 设备离线消息触达的应用；AI 类应用典型场景是"长任务推理完成通知"。

**可借鉴之处（对本项目）**
- **通道选型**：项目当前只有 `google-services.json`（FCM 路径）。国内 OPPO 设备无 GMS，**FCM 不可达**。若要做"回复完成/后台任务结束"通知，OPush 是唯一可靠通道。
- **通知分类**：AI 回复完成属于"用户对接收有预期"的消息，应走**通讯与服务**分类并提前邮件申请 + 提交模板；否则只能走内容与营销，单设备 2 条/日会严重限流。
- **`notify_id` 覆盖**非常适合 AI 场景：同一会话的新回复覆盖旧通知，避免通知栏堆积（也符合审核规范"不可常驻/堆积"的精神）。
- **`verify_registration_id = true`**：防止用户切换后消息错投。
- **延迟初始化**与项目 `mobile_logger.ts` / 设置体系的隐私同意流程要对齐：OPush 的 `init` 必须挂在用户同意之后。Capacitor 插件封装时应把 `init` 暴露成显式方法而非插件自动加载时调用。
- **混淆规则**：项目 release 已开 `minifyEnabled true`，接入 OPush 时必须把两条 `-keep` 写进 `proguard-rules.pro`（当前文件里没有）。
- **未上架商店前只有测试权限**（1000/日，公信），且 2024-07-29 后非商店渠道安装的应用**完全没有公信额度** —— 这意味着灰度/内测分发的包基本收不到通知，测试计划要按"上架后"排。
- 项目已有 `StreamHttpForegroundService` 保活前台流式请求；若改用"后台云推理 + OPush 通知拉起"，可显著降低前台服务依赖，也更容易过审。

---

#### 2.6 《三方应用开发适配指导书》（折叠屏 / 平板 / 大屏）
`https://open.oppomobile.com/new/developmentDoc/info?id=11308`

**核心要点**
- **适配原则**：更大屏幕呈现更多内容；折叠/展开过程中内容完整连续；支持多窗口并行。
- **四步法**：①用放大倍率确定元素显示大小 → ②响应式布局填满屏幕 → ③断点自适应布局 → ④按形态定制。
- **放大倍率**：`getResources().getDisplayMetrics().density`；基准设计宽度 **360dp**；适用于固定尺寸/间距/边距换算，**图片视频不宜统一套用倍率**。
- **响应式四范式**：相对固定（悬浮按钮/底栏/标题栏/TAB/头像）、相对拉伸（文字/搜索框/输入框/卡片容器）、相对缩放（仅沉浸或全屏的固定比例媒体，**勿用于 Banner 与内容型图片**）、内容延伸（横向可滑元素组）。
- **断点与栅格**：宽度断点为主；**小屏 4 栏 / 中屏 8 栏 / 大屏 12 栏**；分屏、面板、扩充层级按各页面实际宽度判定。
- **定制布局**：展开功能菜单、改用**侧边导航**（大屏底部导航过分散）、扩充层级（父子级，适合 **IM、笔记、邮箱**等列表类应用）、悬停适配（"**上部展示内容、下部扩展操作**"）。
- 应用自分屏；首页/影音/阅读等场景的分类型案例。
- **明文要求检查嵌入的 HTML5 内容并缩放到同样的显示大小**，保证整体构成响应式布局。

**适用场景**：折叠屏、平板、多窗口；金标联盟另有《ITGSA 大屏设备应用适配白皮书》（OPPO/vivo/小米联合发布，一次适配多厂生效）。

**可借鉴之处（对本项目）**
- **这一条对 Capacitor WebView 应用价值极高**：文档明确点名"请检查嵌入的 H5 内容并缩放至同样显示大小"——AIbox 整个 UI 就是 WebView 内容，外部 `density` 配置无法替代渲染层自己的响应式。
- 项目是**会话列表 + 对话详情**结构，正是文档点名的"父子级布局"适用场景：中/大屏应做**左侧会话列表 + 右侧对话**双栏，而不是把手机布局拉伸。
- 大屏建议改**侧边导航**，与项目已有的 drawer/sidebar 形态契合，只需在断点处切换常驻。
- 折叠屏半折态：可用 Jetpack WindowManager 监听悬停，做"上屏对话、下屏输入框"——对聊天场景是天然契合的高级玩法。
- 项目 `MainActivity` 的 `configChanges` 已包含 `smallestScreenSize|screenLayout|orientation`，折叠态切换不会重建 Activity ✓，但需确认 renderer 侧有对应的尺寸变化响应。

---

### P2 — 中期能力增强

#### 2.7 《安全服务介绍》
`https://open.oppomobile.com/new/developmentDoc/info?id=11091`

**核心要点**：五项能力 —— 恶意 URL 检测、**日志防泄漏**（对 Android 原生 Logger 输出加密或过滤）、终端环境安全检测（ROOT/模拟器/框架注入/劫持/调试）、隐私安全检测服务（权限分析、SDK 分析、合规整改建议）、**可信数字身份密钥服务**（OPlus Passkeys，基于 FIDO2 的通行密钥，面部/指纹注册登录校验，兼容 Apple/Google/Microsoft Passkeys）。支持 Android 4.4+ 所有主流手机。

**可借鉴之处（对本项目）**
- **日志防泄漏**与项目 `mobile_logger.ts` 直接对口（AGENTS.md 明确要求不落密钥与完整用户内容）。可用于原生层日志兜底过滤。
- **可信数字身份密钥**：AIbox 存有大量用户 API Key（现有 `SecureStoragePlugin` 用 Keystore），若未来做应用锁/敏感设置二次验证，Passkeys 是比自研 PIN 更合规的路径，且跨端生态通用。
- 终端环境安全检测：若未来做付费/订阅，可用于判断支付环境。
- 隐私安全检测服务：上架前自测用，输出整改建议。

---

#### 2.8 《安第斯大模型》与端侧 AI 平台
`https://open.oppomobile.com/new/developmentDoc/info?id=12811`

**核心要点**：AndesGPT 三档规格（Tiny / Turbo / Titan，十亿至千亿参数），能力聚焦知识、记忆、工具、创作；技术特性为对话增强、个性专属、**端云协同**；自研 Swapped Attention（多级缓存历史 KV，降低首字时延，支持 FileChat 文档对话）。端侧部署采用 **1+N LoRA** 架构（一个基模型 + 多个可插拔场景 LoRA）+ QALFT 量化感知训练，75%+ 稀疏度、1.8 BPW，峰值解码加速 6.7 倍。后续开源 **AndesVL** 端侧多模态模型（0.6B–4B 四档，已开源至 HuggingFace / GitHub）。接入入口：开放平台 → 产品列表 → 端侧 AI 申请。

**可借鉴之处（对本项目）**
- **端云协同的调度思路值得直接借鉴**：文档给出的场景划分（弱网/无网用端侧保证核心可用；简单短文本走端侧提速；复杂长文本走云端；隐私相关计算放端侧）可以作为 AIbox"本地小模型 + 云端大模型"混合推理策略的设计参考。
- Swapped Attention 的"历史 KV 缓存复用"对应到 AIbox 就是**多轮长对话的 prompt 缓存**，可作为性能优化方向。
- 若真要调用 OPPO 端侧 NPU，需通过端侧 AI 平台申请，依赖原生插件，成本较高；**当前更现实的价值是架构参考而非直接接入**。

---

#### 2.9 《OPPO 账号 OAuth 授权 SDK》
开启指南 `.../new/developmentDoc/info?id=10154` ｜ 接入指导 `.../new/developmentDoc/info?id=10156`

**核心要点**
- 依赖：`com.heytap.accountsdk:account-oauth-web-sdk:1.1.3`（repo1.maven.org）。
- AndroidManifest `<application>` 下配置 MSP 开放平台生成的 APPID。
- `AcOauthRequest.Builder().setAppId().setAppKey().setScope().setState()` → `AcOauthCallback` 回调取 `authCode`，服务端再用 AppID/AppKey/AppSecret 换 token。
- 能力项需在后台分别申请开通：**openid、公开信息、unionid**。
- 基于 OAuth 2.0，一键授权免密登录。

**可借鉴之处（对本项目）**
- AIbox 当前是"用户自带 API Key"模式，无自有账号体系。若未来要做云同步/订阅，**OPPO 账号一键登录是比手机号验证码更低摩擦的国内登录方案**（免短信成本）。
- 需要注意：AGENTS.md 明确"桌面 OAuth 回调在 Android 需保持不可用"——若接入，OAuth 回调要落在 Capacitor 插件 + deep link（项目已有 `chatbox://` 体系可复用），不要走 localhost 回调。
- 引入新第三方 SDK 意味着隐私政策需同步增补披露项。

---

#### 2.10 接口鉴权接入
开放平台首页 → 开发工具 → 接口鉴权

**核心要点**：开放平台侧的另一套鉴权体系（与 OPush 的 `SHA256(appKey+timestamp+masterSecret)` 不同）。游戏侧的实例为：参数字典序排序 → `key1=value1&key2=value2` 拼接 → **MD5 加密 → 全大写**（参考 `.../new/developmentDoc/info?id=12545`）。

**可借鉴之处**：作为**服务端签名规范的设计参照**：字典序排序 + 键值对拼接 + 摘要 + 时间戳窗口（OPush 10 分钟）+ token 有效期（OPush 24 小时），是厂商侧通行做法。若 AIbox 未来提供自有开放 API，可直接套用这套模式。

---

## 3. 落地建议（按序，当前不改代码）

**第一批 — 合规与稳定性（阻塞上架）**
1. 补 `android:windowLayoutInDisplayCutoutMode`，并在挖孔屏/横屏/折叠屏实测 E2E 与 insets 遮挡（targetSdk 36 是硬风险）。
2. 渲染层做 AI 生成内容标识 + 举报入口 + 青少年模式骨架（审核规范第二十五条）。
3. 按隐私政策清单（id=12277）自检首次启动弹窗、拒绝路径、4 步可达入口、协议独立成文。
4. 在权限合规审核平台上传当前 APK，先看自动解析出哪些敏感权限，提前准备 1080×1920 功能入口截图。
5. 验证 16KB page size 对齐与 64 位包体。

**第二批 — 触达能力**
6. 决策是否需要 OPush。若需要：①企业开发者账号 + 应用创建 + 推送权限申请；②Capacitor 插件封装 `HeytapPushManager`，**`init` 延迟到用户同意后**；③补 proguard 规则；④邮件申请"通讯与服务"分类 + 提交私信模板；⑤服务端按 `/server/v1/auth` 签名规范实现鉴权与 token 刷新。
7. 注意：未上架商店前公信仅 1000/日且非商店渠道包无公信额度，测试计划按"上架后"排。

**第三批 — 体验与适配**
8. 大屏/折叠屏：会话列表 + 对话详情双栏（断点）、大屏侧边导航、H5 内容放大倍率同步。
9. 评估日志防泄漏与 Passkeys（视产品路线）。

---

## 4. 已排除（价值低或不适用）

| 文档 | 排除理由 |
|---|---|
| 主题制作模板 id=10097 | 面向主题设计师，与应用开发无关 |
| 流体云模板 id=12658 / 小布建议 / 负一屏 | 需在系统级入口（状态栏胶囊、锁屏、负一屏）展示服务卡片；AIbox 暂无此触达诉求，成本高、收益不确定 |
| 游戏 SDK / 广告服务 / 钱包服务 / CarLinkUnit / 健康研究 | 与项目形态不匹配 |
| 相册/多媒体/视频/AR 服务 | 当前无对应场景 |
| 应用内支付（OPPO 联运） | 若为订阅制可后续评估，当前非阻塞 |
